package com.app.repository; import com.app.entity.SocietyInspection; import org.springframework.data.jpa.repository.JpaRepository; import java.time.LocalDateTime; import java.util.*;
public interface SocietyInspectionRepository extends JpaRepository<SocietyInspection,Long>{List<SocietyInspection> findByAccountIdOrderByScheduledForDesc(Long accountId);List<SocietyInspection> findByAccountIdAndCompletedAtBetween(Long accountId,LocalDateTime from,LocalDateTime to);Optional<SocietyInspection> findByAccountIdAndId(Long accountId,Long id);long countByAccountIdAndScheduledForBetween(Long accountId,LocalDateTime from,LocalDateTime to);long countByAccountIdAndResult(Long accountId,String result);}
