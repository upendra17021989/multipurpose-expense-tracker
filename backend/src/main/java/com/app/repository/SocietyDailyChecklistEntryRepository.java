package com.app.repository; import com.app.entity.SocietyDailyChecklistEntry; import org.springframework.data.jpa.repository.JpaRepository; import java.time.LocalDate; import java.util.*;
public interface SocietyDailyChecklistEntryRepository extends JpaRepository<SocietyDailyChecklistEntry,Long>{List<SocietyDailyChecklistEntry> findByAccountIdAndOperationDate(Long accountId,LocalDate date);List<SocietyDailyChecklistEntry> findByAccountIdAndOperationDateBetween(Long accountId,LocalDate from,LocalDate to);Optional<SocietyDailyChecklistEntry> findByAccountIdAndTemplateIdAndOperationDate(Long accountId,Long templateId,LocalDate date);}
