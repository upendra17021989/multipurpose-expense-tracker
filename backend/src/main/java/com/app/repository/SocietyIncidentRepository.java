package com.app.repository; import com.app.entity.SocietyIncident; import org.springframework.data.jpa.repository.JpaRepository; import java.time.LocalDateTime; import java.util.*;
public interface SocietyIncidentRepository extends JpaRepository<SocietyIncident,Long>{List<SocietyIncident> findByAccountIdOrderByOccurredAtDesc(Long accountId);Optional<SocietyIncident> findByAccountIdAndId(Long accountId,Long id);long countByAccountId(Long accountId);long countByAccountIdAndStatusNot(Long accountId,String status);long countByAccountIdAndOccurredAtBetween(Long accountId,LocalDateTime from,LocalDateTime to);}
