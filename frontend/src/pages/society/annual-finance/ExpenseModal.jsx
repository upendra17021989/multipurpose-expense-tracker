import { useEffect,useState } from 'react'
import { toast } from 'react-toastify'
import { expenseAPI,expenseCategoryAPI } from '../../../api/endpoints'
const blank=y=>({expenseDate:`${y.slice(0,4)}-04-01`,categoryId:'',expenseType:'SOCIETY',vendorName:'',description:'',amount:'',paymentMode:'UPI',status:'DRAFT',remarks:''})
export const ExpenseModal=({open,financialYear,expense,onClose,onSaved})=>{const[form,setForm]=useState(blank(financialYear)),[categories,setCategories]=useState([]),[saving,setSaving]=useState(false);useEffect(()=>{if(open){setForm(expense?{...blank(financialYear),...expense,categoryId:expense.categoryId||''}:blank(financialYear));expenseCategoryAPI.getCategories().then(r=>setCategories(r.data||[])).catch(()=>toast.error('Unable to load categories'))}},[open,financialYear,expense]);if(!open)return null
const submit=async e=>{e.preventDefault();setSaving(true);try{const payload={...form,categoryId:Number(form.categoryId),amount:Number(form.amount),vendorName:form.vendorName||null,description:form.description||null,remarks:form.remarks||null,festivalEventId:null};expense?await expenseAPI.updateExpense(expense.id,payload):await expenseAPI.createExpense(payload);toast.success(expense?'Expense updated':'Expense created');onSaved();onClose()}catch(error){toast.error(error.response?.data?.message||'Unable to save expense')}finally{setSaving(false)}}
return <div className="modal-backdrop" onMouseDown={()=>!saving&&onClose()}><section className="expense-modal annual-finance-modal" role="dialog" aria-modal="true" onMouseDown={e=>e.stopPropagation()}><div className="expense-modal-header"><div><h2>{expense?'Edit Expense':'Add Expense'}</h2><p className="muted">Financial year {financialYear}</p></div><button className="modal-close" onClick={onClose}>×</button></div><form onSubmit={submit}><div className="expense-modal-form"><label>Date<input required type="date" min={`${financialYear.slice(0,4)}-04-01`} max={`${financialYear.slice(5)}-03-31`} value={form.expenseDate} onChange={e=>setForm({...form,expenseDate:e.target.value})}/></label><label>Category<select required value={form.categoryId} onChange={e=>setForm({...form,categoryId:e.target.value})}><option value="">Select category</option>{categories.map(x=><option key={x.id} value={x.id}>{x.categoryName}</option>)}</select></label><label>Vendor<input value={form.vendorName||''} onChange={e=>setForm({...form,vendorName:e.target.value})}/></label><label>Description<input value={form.description||''} onChange={e=>setForm({...form,description:e.target.value})}/></label><label>Amount<input required type="number" min="0.01" step="0.01" value={form.amount} onChange={e=>setForm({...form,amount:e.target.value})}/></label><label>Payment mode<select value={form.paymentMode} onChange={e=>setForm({...form,paymentMode:e.target.value})}>{['CASH','BANK','UPI','CARD','NEFT','CHEQUE'].map(x=><option key={x}>{x}</option>)}</select></label><label>Status<select value={form.status} onChange={e=>setForm({...form,status:e.target.value})}>{['DRAFT','SUBMITTED','PAID'].map(x=><option key={x}>{x}</option>)}</select></label><label>Remarks<input value={form.remarks||''} onChange={e=>setForm({...form,remarks:e.target.value})}/></label></div><div className="expense-modal-actions"><button type="button" onClick={onClose}>Cancel</button><button className="primary" disabled={saving}>{saving?'Saving...':expense?'Save Changes':'Add Expense'}</button></div></form></section></div>}
